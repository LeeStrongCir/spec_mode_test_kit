# 软件测试计划与测试设计融合指南

> 目标：一份简洁但重点明确的资料，覆盖测试策略、测试计划、测试设计三大维度，可直接用于项目实践。

---

## 一、测试策略：测试金字塔

### 核心原则

**分层测试，自底向上，逐层减少。** 底层快速反馈，高层建立信心。

```
        ┌───────────┐
        │  E2E 测试  │  ← 少量（5-10%），高信心，慢
        ├───────────┤
        │ 集成/合同  │  ← 适中（20-30%），验证协作
        ├───────────┤
        │  单元测试   │  ← 大量（60-75%），快速，隔离
        └───────────┘
```

### 各层定位

| 层级 | 目标 | 速度 | 数量比 | 关键实践 |
|------|------|------|--------|----------|
| **单元测试** | 验证单一函数/类的正确性 | <100ms | 60-75% | Mock 外部依赖；一个断言一个场景 |
| **集成测试** | 验证模块间协作（DB、API、消息队列） | <1s | 15-25% | 用真实 DB（测试容器）；stub 外部服务 |
| **合同测试** | 验证服务间接口契约不被破坏 | <500ms | 5-10% | Consumer-driven（Pact）；双向验证 |
| **E2E 测试** | 验证用户完整业务流程 | 数秒~数十秒 | <5% | 只覆盖关键路径；API 驱动优先于 UI 驱动 |

### 反模式

- **冰淇淋锥**（Ice Cream Cone）：大量 E2E + 少量单元测试 → 慢、脆弱、难维护
- **测试孤岛**（Test Island）：只有集成测试，没有单元和 E2E
- **过度 Mock**：Mock 了太多实现细节，测试变成代码的镜像而非行为的验证

---

## 二、测试计划：什么时间做什么

### 测试计划模板（精简版）

```markdown
# 测试计划：[项目/功能名]

## 1. 测试范围
- 包含：[列出要测试的功能模块]
- 排除：[列出不测试的内容及理由]

## 2. 测试层级与策略
| 层级 | 工具/框架 | 负责人 | 执行时机 |
|------|----------|--------|----------|

## 3. 测试环境
- 开发环境：[描述]
- 测试环境：[描述]
- 数据策略：[如何准备和清理测试数据]

## 4. 准入/准出标准
- 准入：[代码合并前需满足的条件]
- 准出：[发布前需满足的条件]

## 5. 风险与缓解
| 风险 | 影响 | 缓解措施 |
|------|------|----------|

## 6. 进度与里程碑
| 阶段 | 交付物 | 截止日期 |
|------|--------|----------|
```

### 关键决策点

**准入标准**（Merge/PR 前）：
- 单元测试通过率 100%
- 代码覆盖率 ≥ 80%（新功能代码）
- 静态分析无 Critical/Blocker 级别问题
- 集成测试通过（CI 自动）

**准出标准**（Release 前）：
- 所有 P0/P1 缺陷已修复或已批准延期
- E2E 关键路径 100% 通过
- 性能测试满足 SLA 要求
- 安全扫描无高危漏洞
- 回归测试通过（全量或基于风险裁剪）

---

## 三、测试设计：如何设计测试用例

### 3.1 黑盒测试设计技术（Specification-Based）

#### 等价类划分（Equivalence Partitioning）

**核心**：将输入域划分为若干等价类，每类选一个代表值。

| 步骤 | 操作 |
|------|------|
| 1 | 识别输入条件（如：年龄 18-65） |
| 2 | 划分有效等价类（18-65）和无效等价类（<18, >65） |
| 3 | 每类选一个代表性值 |

**示例**：用户年龄输入 18-65

| 等价类 | 测试值 | 预期 |
|--------|--------|------|
| 有效：18-65 | 30 | 通过 |
| 无效：<18 | 17 | 拒绝 |
| 无效：>65 | 66 | 拒绝 |

#### 边界值分析（Boundary Value Analysis）

**核心**：缺陷最可能出现在边界。每个边界测试边界值及其相邻值。

**规则**：输入范围 [a, b] → 测试 a-1, a, a+1, b-1, b, b+1

**示例**：密码长度 8-20 字符

| 测试值 | 类型 | 预期 |
|--------|------|------|
| 7 | 下边界外 | 拒绝 |
| 8 | 下边界 | 通过 |
| 9 | 下边界内 | 通过 |
| 19 | 上边界内 | 通过 |
| 20 | 上边界 | 通过 |
| 21 | 上边界外 | 拒绝 |

#### 判定表（Decision Table Testing）

**核心**：适用于多条件组合的业务规则。列出所有条件组合及对应动作。

**示例**：订单折扣规则

| 条件 | VIP | 金额>1000 | 节假日 |
|------|-----|-----------|--------|
| **规则1** | ✓ | ✓ | ✓ | → 8折 |
| **规则2** | ✓ | ✓ | ✗ | → 8.5折 |
| **规则3** | ✓ | ✗ | ✓ | → 9折 |
| **规则4** | ✗ | ✓ | ✓ | → 9折 |
| **规则5** | 任意 | 任意 | ✗ | → 无折扣 |

→ 5 条规则 = 5 个测试用例

#### 状态转换测试（State Transition Testing）

**核心**：对有状态的系统，测试所有合法状态转换，同时测试非法转换。

**示例**：订单状态机

```
待支付 → 已支付 → 发货中 → 已送达 → 已完成
  ↓        ↓
已取消   退款中 → 已退款
```

| 测试用例 | 当前状态 | 触发事件 | 预期结果 |
|----------|----------|----------|----------|
| TC-01 | 待支付 | 支付成功 | → 已支付 |
| TC-02 | 待支付 | 取消订单 | → 已取消 |
| TC-03 | 已送达 | 支付成功 | 拒绝（非法转换） |
| TC-04 | 已完成 | 发货 | 拒绝（非法转换） |

#### 用例测试（Use Case Testing）

**核心**：从用户视角，覆盖主流程 + 备选流程。

| 部分 | 内容 |
|------|------|
| 主流程 | 用户成功完成目标的标准路径 |
| 备选流程1 | 用户取消操作 |
| 备选流程2 | 系统异常/超时 |
| 备选流程3 | 权限不足 |

### 3.2 白盒测试设计技术（Structure-Based）

#### 语句覆盖（Statement Coverage）

每个可执行语句至少执行一次。**最低标准，通常要求 ≥ 80%。**

#### 判定覆盖（Branch Coverage）

每个判断的真/假分支至少各执行一次。**推荐标准，通常要求 ≥ 80%。**

#### 条件覆盖（Condition Coverage）

每个布尔子表达式取真/假各至少一次。

**示例**：`if (age >= 18 && hasId)`

| 测试用例 | age >= 18 | hasId | 覆盖 |
|----------|-----------|-------|------|
| age=20, hasId=true | ✓ | ✓ | 全真 |
| age=15, hasId=true | ✗ | ✓ | 假+真 |
| age=20, hasId=false | ✓ | ✗ | 真+假 |
| age=15, hasId=false | ✗ | ✗ | 全假 |

#### 路径覆盖（Path Coverage）

覆盖所有可能的执行路径。复杂度高，通常只覆盖关键路径。

### 3.3 经验驱动测试设计

| 技术 | 适用场景 | 做法 |
|------|----------|------|
| **错误推测**（Error Guessing） | 有经验的测试人员 | 基于历史缺陷模式：空指针、除零、并发竞争、时区问题 |
| **探索性测试**（Exploratory Testing） | 需求模糊/快速迭代 | 同时学习、设计、执行测试；时间盒（time-box）方法 |
| **检查表测试**（Checklist-Based） | 回归测试/发布前 | 基于经验积累的检查清单：安全性、性能、兼容性、无障碍 |

### 3.4 高级测试设计技术

#### 成对测试（Pairwise Testing）

**场景**：多参数组合，全排列爆炸。

**方法**：确保每对参数值至少组合一次。

**示例**：3 个参数，每个 2 个值（全排列 8 种）

| 用例 | OS | Browser | Theme |
|------|----|---------|-------|
| 1 | Windows | Chrome | Light |
| 2 | Windows | Firefox | Dark |
| 3 | Mac | Chrome | Dark |
| 4 | Mac | Firefox | Light |

→ 4 个用例覆盖所有参数对（vs 全排列 8 个）

#### 属性测试（Property-Based Testing）

**核心**：不指定具体输入输出，而是定义系统应满足的**属性/不变量**，由测试框架自动生成大量随机输入来验证。

**经典属性**：
- **往返**（Round-trip）：`decode(encode(x)) == x`
- **幂等**（Idempotent）：`f(f(x)) == f(x)`
- **交换**（Commutative）：`f(a, b) == f(b, a)`
- **不变量**（Invariant）：排序后 `array[i] <= array[i+1]`

#### 变异测试（Mutation Testing）

**核心**：在代码中注入微小变异（如 `>` → `>=`，`+` → `-`），验证测试能否发现（杀死变异体）。

**指标**：变异得分（Mutation Score）= 杀死的变异体数 / 总变异体数

**目标**：≥ 80%。比代码覆盖率更能反映测试质量。

---

## 四、测试设计实战工作流

### 从需求到测试用例的完整链路

```
需求/用户故事
    ↓
1. 识别测试条件（What to test）
    ↓
2. 选择测试设计技术
   ├── 输入验证 → 等价类 + 边界值
   ├── 业务规则 → 判定表
   ├── 状态系统 → 状态转换
   ├── 用户流程 → 用例测试
   └── 多参数  → 成对测试
    ↓
3. 生成测试用例（Test Cases）
    ↓
4. 确定优先级（P0/P1/P2）
    ↓
5. 编写测试代码 / 测试脚本
    ↓
6. 执行 & 记录结果
```

### 优先级划分

| 级别 | 定义 | 覆盖范围 | 执行频率 |
|------|------|----------|----------|
| **P0** | 核心功能，失败导致系统不可用 | 关键路径 100% | 每次提交 |
| **P1** | 重要功能，有替代方案 | 主要功能 100% | 每日 |
| **P2** | 次要功能/边界场景 | 部分覆盖 | 每周/迭代末 |

---

## 五、各层测试的设计要点

### 单元测试设计

- **一个测试一个断言**（或一组相关断言）
- **命名**：`test_[方法名]_[场景]_[预期结果]`
- **AAA 模式**：Arrange（准备）→ Act（执行）→ Assert（验证）
- **只 Mock 外部**：不 Mock 同一模块内的协作类
- **测试什么**：业务逻辑、边界条件、异常路径

### 集成测试设计

- **测试真实依赖**：用测试容器（Testcontainers）启动真实 DB/消息队列
- **Stub 外部服务**：用 WireMock 模拟第三方 API
- **测试什么**：SQL 正确性、API 调用、事务、序列化/反序列化

### 合同测试设计

- **Consumer 驱动**：消费者定义期望，提供者验证
- **测试什么**：请求格式、响应格式、错误码、必填字段
- **工具**：Pact、Spring Cloud Contract

### E2E 测试设计

- **只测关键路径**：注册→登录→核心操作→退出
- **API 优先**：优先用 REST API 驱动，UI 测试只用于前端交互验证
- **数据隔离**：每次测试用独立数据，不依赖其他测试的数据
- **测试什么**：端到端业务流程、UI 交互、权限流程

---

## 六、非功能性测试设计

### 性能测试

| 类型 | 目标 | 方法 |
|------|------|------|
| 负载测试 | 正常/峰值负载下的表现 | 逐步增加并发用户 |
| 压力测试 | 系统崩溃点 | 持续增加负载直到失败 |
| 耐力测试 | 长时间运行的稳定性 | 恒定负载运行 24-72 小时 |
| 尖峰测试 | 突发流量应对 | 瞬间从低负载跳到高负载 |

**关键指标**：响应时间（P50/P95/P99）、吞吐量（QPS/TPS）、错误率、资源利用率

### 安全测试

| 检查项 | 方法 |
|--------|------|
| 认证/授权 | 越权访问、token 篡改、会话固定 |
| 注入攻击 | SQL 注入、XSS、命令注入 |
| 数据安全 | 敏感数据加密、日志脱敏 |
| 配置安全 | 默认密码、暴露的调试端点、CORS 配置 |

### 兼容性测试

| 维度 | 检查项 |
|------|--------|
| 浏览器 | Chrome、Firefox、Safari、Edge（最新 2 个版本） |
| 设备 | 手机（375px）、平板（768px）、桌面（1440px） |
| OS | Windows、macOS、iOS、Android |
| 网络 | 3G、4G、WiFi（网络节流测试） |

---

## 七、CI/CD 中的测试流水线

```
代码提交
  │
  ├─→ 预提交钩子：lint + 格式化
  │
  ├─→ CI 流水线：
  │     ├─ 单元测试（~5min）    ← 阻断合并
  │     ├─ 静态分析（~2min）    ← 阻断合并
  │     ├─ 集成测试（~10min）   ← 阻断合并
  │     └─ 覆盖率报告           ← 信息性
  │
  ├─→ PR 合并后：
  │     ├─ E2E 测试（~15min）   ← 阻断部署
  │     ├─ 性能基准测试          ← 信息性（回归告警）
  │     └─ 安全扫描             ← 阻断部署
  │
  └─→ 部署后：
        ├─ 冒烟测试              ← 阻断发布
        ├─ 健康检查              ← 自动回滚
        └─ 生产监控              ← 告警
```

---

## 八、测试数据管理

### 原则

1. **可重现**：测试数据应在每次测试前确定性地生成
2. **独立性**：测试之间不共享数据，不依赖执行顺序
3. **最小化**：只创建测试需要的最少数据

### 策略

| 策略 | 适用场景 | 做法 |
|------|----------|------|
| **工厂方法** | 单元测试 | 用工厂函数生成测试数据 |
| **测试数据 Builder** | 复杂对象 | Builder 模式，默认值 + 可选覆盖 |
| **种子数据** | 集成测试 | 每次测试前用 Flyway/Liquibase 迁移 |
| **数据工厂服务** | E2E 测试 | 专用 API 创建测试数据，测试后清理 |

---

## 九、检查清单：发布前必查

### 功能性
- [ ] P0 测试用例 100% 通过
- [ ] 已知缺陷已评估且批准
- [ ] 回归测试通过

### 非功能性
- [ ] 性能指标在 SLA 范围内
- [ ] 安全扫描无高危漏洞
- [ ] 兼容性检查通过（目标浏览器/设备）

### 代码质量
- [ ] 单元测试覆盖率 ≥ 80%（新功能）
- [ ] 静态分析无 Critical 问题
- [ ] 代码审查通过

### 部署准备
- [ ] 数据库迁移脚本已测试
- [ ] 回滚方案已准备
- [ ] 监控告警已配置
- [ ] 文档已更新

---

## 十、常见陷阱与应对

| 陷阱 | 表现 | 应对 |
|------|------|------|
| 测试雪崩 | 测试太多，CI 跑不动 | 按优先级分层执行；P0 每次提交，P2 每日/每周 |
| 脆弱测试 | 代码未变，测试随机失败 | 消除时序依赖；用等待/重试替代 sleep |
| 测试即文档 | 测试代码成了唯一文档 | 测试名即文档；复杂逻辑加注释 |
| 100% 覆盖执念 | 为了覆盖率写无意义测试 | 关注行为覆盖，不是行覆盖；用变异测试验证 |
| 测试环境漂移 | 测试通过，上线失败 | 环境即代码（IaC）；测试环境与生产环境一致 |
| 测试数据污染 | 测试互相干扰 | 每次测试独立数据；测试后清理 |

---

## 附录：测试用例模板

```markdown
## TC-[编号]: [测试用例名称]

**优先级**: P0 / P1 / P2
**类型**: 单元 / 集成 / E2E / 性能 / 安全
**设计技术**: 等价类 / 边界值 / 判定表 / 状态转换 / 用例 / 探索性

### 前置条件
- [条件1]
- [条件2]

### 测试步骤
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | [操作] | [预期] |
| 2 | [操作] | [预期] |

### 测试数据
| 参数 | 值 | 来源 |
|------|----|------|

### 后置条件
- [清理操作]
```

---

*本文档基于 ISTQB 标准、Martin Fowler 测试金字塔实践、Google/Microsoft 工程实践及行业经验整理。*
