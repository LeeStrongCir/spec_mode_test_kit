{
  "name": "lee-cloud-test-suite",
  "version": "1.0.0",
  "description": "Test suite for Lee Cloud Console - functional tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=html",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
